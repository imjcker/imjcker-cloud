package com.imjcker.repository;

import com.imjcker.domain.RequestParam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestParamRepository extends JpaRepository<RequestParam, Integer> {
}
