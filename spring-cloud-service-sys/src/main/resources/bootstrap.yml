spring:
  application:
    name: spring-cloud-service-sys
  cloud:
    config:
      discovery:
        enabled: true
        service-id: spring-cloud-config-server
      uri: http://imjcker.com:8888
      name: ${spring.application.name}
      profile: prod
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://imjcker.com:8761/eureka/

